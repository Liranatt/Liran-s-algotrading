# 1. Ignore everything by default
*

# 2. But do NOT ignore directories, so Git can see their contents
!*/

# 3. And do NOT ignore these specific file types
!*.py
!*.csv
!*.txt
!*.log

# 4. Also, don't ignore the .gitignore file itself!
!.gitignore